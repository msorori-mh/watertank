[install]
# Seven-day supply-chain guard: avoid newly published package versions by default.
minimumReleaseAge = 604800
# Each entry bypasses the guard for one package — keep this list narrowly reviewed.
# before adding any.
minimumReleaseAgeExcludes = ["@lovable.dev/vite-tanstack-config"]
